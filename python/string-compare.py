# Порівняння рядків

# ==
# !=
# >
# >=
# <
# <=

'alpha' == 'alpha'
'alpha' != 'Alpha'
'alpha' < 'alphabet'

# Рядки можна порівнювати з іншими рядками за допомогою загальних операторів порівняння,
# але порівняння їх із числами не дає розумного результату,
# оскільки жоден рядок не може дорівнювати жодному числу. Наприклад:

# рядок == число є завждипомилковий;
# рядок != число є завждиправда;
# рядок >= число завжди створює виняток .
