# Функція repr() повертає представлення вказаного об’єкта, яке можна вивести.

numbers = [1, 2, 3, 4, 5]

# Створюємо представлення списку, яке можна вивести на екран
printable_numbers = repr(numbers)
print(printable_numbers)
